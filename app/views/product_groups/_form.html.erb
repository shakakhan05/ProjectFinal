<%= form_for(@product_group) do |f| %>
  <% if @product_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_group.errors.count, "error") %> prohibited this product_group from being saved:</h2>

      <ul>
      <% @product_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <center>
						<div id="newuser_border" class="radius_super">
							<div id="login_panel" class="radius_big">
								<table width="100px">
									<tr>
										
									</tr>

  <div class="index_label" >
    <%= f.label :prodgrpid %><br />
    <%= f.number_field :prodgrpid %>
  </div>
  <div class="index_label" >
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit 'Create Product Group', :class => 'button' %>
  </div>
<% end %>
